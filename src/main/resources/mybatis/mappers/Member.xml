<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="itc.hoseo.oversea.mapper.XMLConfigMemberMapper">
  <select id="getMemberName" resultType="String">
    select name from user where email = #{email}
  </select>
  <select id="getMemberCount" resultType="int">
  	select count(*) from user
  </select>
  <insert id="addMember" parameterType="itc.hoseo.oversea.member.Member">
  	INSERT INTO user(name, email, passwd, phone, birth, zipcode, addr) 
  	VALUES (#{name}, #{email}, #{passwd}, #{phone}, #{birth}, #{zipcode}, #{addr})
  </insert>
</mapper>